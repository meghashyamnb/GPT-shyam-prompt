READ-ONLY QA DOCUMENTATION TASK.

Do not edit, create, delete or rename any repository files.
Do not modify application code.
Do not modify the existing Postman collection.
Do not add logging.
Do not execute deployments or destructive commands.
Inspect the repository and existing Postman collection only, then return the final manual Xray test cases in chat.

Act as a senior API QA/SDET experienced in Node.js, Moleculer, cache-manager, Postman, OpenShift, Splunk and Xray.

I need final, readable and manually executable Xray test cases for the cache TTL fix.

The Xray project supports these step columns:

1. Action
2. Data
3. Expected Result

Every test step must use exactly these columns.

The test cases must be detailed enough that I can:

- Open the correct existing Postman request.
- Set the correct request data and headers.
- Execute the request.
- Capture the Postman response.
- Retrieve relevant OpenShift pod logs.
- Search the same evidence in Splunk.
- Compare the expected cumulative event counts.
- Attach evidence confidently to the Xray execution.

Do not give high-level statements such as:

- Call the API.
- Verify cache.
- Check logs.
- Response should be faster.
- Cache should work.

Give exact step-by-step execution instructions.

==================================================
CONFIRMED TECHNICAL CONTEXT
==================================================

Use these confirmed facts unless the repository directly proves that a value is different.

Affected caches:

1. System Authority access-token memory cache.
2. Third-party configuration memory cache.

Cache behaviour:

- Both caches are module-level in-process memory caches.
- Both caches are local to each application pod.
- Restarting the pod provides a clean-cache state.
- The existing DELETE /cache route clears only the Moleculer broker cache.
- DELETE /cache does not clear the affected token or third-party config caches.
- Do not use DELETE /cache as the setup for affected cache tests.

TTL defect:

- The intended TTL was 3600 seconds.
- The previous broken behaviour treated 3600 as milliseconds.
- This caused expiry after approximately 3.6 seconds.
- The corrected implementation multiplies seconds by 1000.

System Authority token TTL:

JWT exp
- current epoch seconds
- ttlBufferInSeconds
then
× 1000

Confirmed token buffer:

ttlBufferInSeconds = 5 seconds

Third-party config TTL:

thirdPartiesCacheTtlInSeconds × 1000

Default config TTL:

3600 seconds × 1000 = 3,600,000 milliseconds

Feature-branch package versions identified:

- ob-system-authority-moleculer: 1.0.5
- ob-system-authority: 1.0.6
- lib-ob-third-party-config-mixin: 9.0.8

Primary business request identified:

GET /open-banking-anz/party

The exact Postman collection information discovered previously appears to be:

- Collection: Open Banking - Accounts
- Folder: 02d - Party
- Request: Get Party

Recheck these values against the actual Postman collection before using them.

==================================================
FIRST: INSPECT THE ACTUAL REPOSITORY AND COLLECTION
==================================================

Before producing test cases, inspect:

1. The complete existing Postman collection.
2. All route definitions.
3. Moleculer services and actions.
4. Third-party enquiry mixin.
5. System Authority wrapper and underlying package calls.
6. Cache logging added to the feature branch.
7. OpenShift deployment configuration.
8. Existing Splunk log fields.
9. Existing WireMock setup, where available.
10. Existing automated tests.

Identify the exact:

- Postman collection name.
- Folder name.
- Request name.
- HTTP method.
- URL.
- Environment-variable names.
- Required headers.
- Request parameters.
- Stable response fields.
- Correlation-ID header.
- OpenShift namespace.
- Deployment name.
- Container name, if required.
- Confirmed cache event names.
- Confirmed token event type IDs.
- Splunk index, source and pod-name fields, where present.
- Business endpoints affected by either cache.

Do not invent missing values.

Where something cannot be found, write:

QA to confirm before execution: <missing value>

==================================================
LOCKED LOG-EVIDENCE RULES
==================================================

Use only event names that actually exist in the deployed feature branch.

Known token events may include:

- TOKEN_LOOKUP_STARTED
- SAT_TOKEN_REQUESTED_FROM_SYS_AUTH
- typeId 1001820
- SAT_TOKEN_RECEIVED
- typeId 1001890
- TOKEN_LOOKUP_COMPLETE

A token cache hit may not have a direct TOKEN_CACHE_HIT event.

Therefore, token reuse must be proven as:

1. The repeated business request succeeds.
2. No new SAT_TOKEN_REQUESTED_FROM_SYS_AUTH event appears.
3. The cumulative typeId 1001820 count for the same pod and exact test time window remains unchanged.

Do not invent TOKEN_CACHE_HIT when it is not emitted by the implementation.

Known third-party configuration events may include:

- TP_CONFIG_CACHE_LOOKUP
- TP_CONFIG_CACHE_MISS
- TP_CONFIG_DOWNSTREAM_REQUEST
- TP_CONFIG_CACHE_STORED
- TP_CONFIG_CACHE_HIT
- TP_CONFIG_CACHE_FETCH_FAILED

Use only confirmed events.

A config cache hit must be proven using:

- TP_CONFIG_CACHE_HIT, where available.
- No new TP_CONFIG_DOWNSTREAM_REQUEST.
- Cumulative config downstream count remains unchanged.

Response duration is supporting evidence only.

Do not define a value such as durationMs <= 5 as the pass criterion.

Never attach or require:

- Full JWT.
- Full access token.
- Client secret.
- Authorization header.
- Complete token cache key.
- Full third-party configuration JSON.
- Customer-sensitive information.

For the config store event, capture only safe metadata such as:

- Event name.
- Safe thirdPartyId.
- ttlMs.
- configuredTtlSeconds.
- expiresAtEpochMs, where present.
- Pod name.
- Correlation ID.

==================================================
XRAY TEST-CASE FORMAT
==================================================

For every test, return exactly:

Test ID:
Title:
Priority:
Objective:

Preconditions:
- Detailed setup requirement 1
- Detailed setup requirement 2
- Detailed setup requirement 3

Test Steps:

| Step | Action | Data | Expected Result |
|------|--------|------|-----------------|
| 1 | Exact tester instruction | Exact input/settings | Exact observable outcome |
| 2 | Exact tester instruction | Exact input/settings | Exact observable outcome |

Evidence to attach:
- Exact Postman evidence
- Exact OpenShift evidence
- Exact Splunk evidence
- WireMock evidence only when required

Pass criteria:
- One precise measurable statement.

Fail criteria:
- One precise measurable statement.

==================================================
HOW TO WRITE PRECONDITIONS
==================================================

Every cache test precondition must clearly state:

- Environment and namespace.
- Feature-branch deployment/image requirement.
- Correct package-version requirement.
- Replica count.
- Same-pod requirement.
- Pod restart requirement.
- Pod Ready-state requirement.
- Cache logging requirement.
- Test data.
- Whether WireMock or real Dev downstream is used.
- Required token/config TTL.
- Exact existing Postman request.
- Test start-time recording requirement.
- No unrelated traffic during the test.

Example style:

Preconditions:

- Feature branch containing the fixed package versions is deployed in <confirmed namespace>.
- Deployment is scaled to one replica.
- The application pod is restarted and the new pod reaches 1/1 Running.
- Record the new pod name and UTC test start time.
- CACHE_TEST_LOGGING is enabled where applicable.
- Use existing Postman collection <exact collection>.
- Use existing request <exact request>.
- Use the same thirdPartyId and client credentials for repeated requests.
- No other tester or automated job should invoke the same token/config flow during the execution window.

==================================================
HOW TO WRITE POSTMAN ACTIONS
==================================================

Every Postman step must tell the tester:

1. Which collection to open.
2. Which folder to open.
3. Which exact existing request to select.
4. Which environment to select.
5. Which existing variables to verify.
6. Which headers to set.
7. Which values must remain unchanged.
8. Which correlation ID must be changed.
9. When to click Send.
10. What to record from the response.

Example:

Action:

Open Postman. Select collection “Open Banking - Accounts”, open folder “02d - Party” and select request “Get Party”. Select the confirmed Dev environment. Verify all required variables are resolved. Set a new x-fapi-interaction-id and click Send.

Data:

- Method: GET
- Path: /open-banking-anz/party
- x-fapi-interaction-id: CACHE-002-REQ-1
- x-oba-consent: existing valid collection value
- x-oba-enriched-jwt: existing valid collection value
- thirdPartyId: <confirmed existing variable/value>
- All remaining headers: unchanged from the existing request

Expected Result:

- HTTP 200 is returned.
- The response schema is valid.
- Stable party fields are populated.
- Record response timestamp and correlation ID.
- Do not expose complete token/header values in the screenshot.

==================================================
HOW TO WRITE OPENSHIFT LOG ACTIONS
==================================================

Do not instruct QA to continuously watch all logs.

Use this approach:

1. Record UTC test start time.
2. Execute the Postman requests.
3. Retrieve logs only from the test start time.
4. Filter by relevant cache/token events.
5. Save or screenshot the filtered result.

Use commands based on confirmed deployment details.

Template:

START_TIME=$(date -u +"%Y-%m-%dT%H:%M:%SZ")

oc logs <POD_NAME> \
  -n <NAMESPACE> \
  --since-time="$START_TIME" \
  --timestamps=true |
grep -E "1001820|1001890|TOKEN_LOOKUP|TP_CONFIG_CACHE|TP_CONFIG_DOWNSTREAM"

If the pod contains multiple containers, include the confirmed application container:

-c <APPLICATION_CONTAINER>

For every command:

- Use the exact namespace.
- Use the exact pod name.
- Use the exact container where applicable.
- State the exact event being counted.
- State the expected cumulative count.

Do not use an unrestricted full-day log search.

==================================================
HOW TO WRITE SPLUNK ACTIONS
==================================================

For each relevant test, include a readable Splunk step.

The Splunk search must be limited using:

- Application/service.
- Namespace.
- Exact pod name.
- Test start and end timestamps.
- Correlation ID where propagated.
- Exact token/cache event names.

Use the confirmed field names from existing Splunk events.

Where index and field names cannot be found, use:

QA to confirm before execution:
- Splunk index
- pod-name field
- namespace field
- application/source field

The Expected Result must show cumulative counts, for example:

- typeId 1001820 count = 1.
- typeId 1001890 count = 1.
- TP_CONFIG_DOWNSTREAM_REQUEST count = 1.
- TP_CONFIG_CACHE_HIT count = 1.

==================================================
STABLE RESPONSE-COMPARISON RULE
==================================================

Do not require the complete second response body to exactly match the first response.

Compare only:

- HTTP status.
- Response schema.
- Stable business identifiers.
- Stable party/account/config marker fields.
- Required array/object presence.

Ignore dynamic values such as:

- Correlation ID.
- Response timestamps.
- Trace IDs.
- Generated metadata.
- Ordering where ordering is not guaranteed.

==================================================
LOCKED TEST CASES
==================================================

Generate these tests only.

TC-CACHE-001 — Verify fixed package versions are deployed

TC-CACHE-002 — Verify System Authority token and third-party config are reused before expiry

TC-CACHE-003 — Verify both caches survive beyond the old 3.6-second boundary

TC-CACHE-004 — Verify System Authority token refreshes after effective expiry

TC-CACHE-005 — Verify five-second token cache buffer

TC-CACHE-006 — Verify third-party configuration refreshes after TTL

TC-CACHE-007 — Verify third-party config cache-key isolation

TC-CACHE-008 — Verify token downstream failure is not cached

TC-CACHE-009 — Verify third-party configuration failure is not cached

TC-CACHE-010 — Verify impacted business endpoints continue to work

Do not create additional test IDs.

==================================================
TC-CACHE-001 REQUIRED STEPS
==================================================

Include detailed steps to verify:

1. Feature-branch commit/image is deployed.
2. Pod is running the intended image.
3. package-lock.json resolves:
   - ob-system-authority-moleculer 1.0.5
   - ob-system-authority 1.0.6
   - lib-ob-third-party-config-mixin 9.0.8
4. Token implementation multiplies TTL by 1000.
5. Config implementation multiplies TTL by 1000.
6. Cache-test environment variables are deployed where applicable.

Expected Result must state:

The version and implementation evidence confirms that the fix is present, but runtime behaviour is validated by TC-CACHE-002 onward.

==================================================
TC-CACHE-002 REQUIRED EXECUTION
==================================================

Objective:

Prove first request causes downstream fetch and the immediate second request reuses both caches.

Execution:

1. Scale deployment to one pod.
2. Restart the application pod.
3. Wait for 1/1 Running.
4. Record pod name and UTC test start time.
5. Run the exact existing Get Party Postman request.
6. Record HTTP response and correlation ID.
7. Retrieve token logs.
8. Confirm cumulative 1001820 count = 1.
9. Confirm cumulative 1001890 count = 1.
10. Confirm:
    - TP_CONFIG_CACHE_MISS = 1
    - TP_CONFIG_DOWNSTREAM_REQUEST = 1
    - TP_CONFIG_CACHE_STORED = 1
11. Send the same Postman request again within two seconds.
12. Change only the interaction/correlation ID.
13. Confirm HTTP 200 and stable business fields.
14. Confirm cumulative 1001820 count remains 1.
15. Confirm config downstream count remains 1.
16. Confirm TP_CONFIG_CACHE_HIT for the second request, where emitted.
17. Confirm no new config miss/store event for the second request.

==================================================
TC-CACHE-003 REQUIRED EXECUTION
==================================================

Objective:

Prove both caches survive beyond the old 3.6-second boundary.

Execution:

1. Restart one pod.
2. Record start time.
3. Send Get Party request at T+0.
4. Confirm token and config downstream counts are each 1.
5. Wait exactly 10 seconds.
6. Send the same request with a new interaction ID.
7. Confirm HTTP 200.
8. Confirm token count remains 1.
9. Confirm config downstream count remains 1.
10. Confirm config cache-hit evidence.

Expected Result must state:

This proves survival beyond the previous 3.6-second defect boundary. It does not prove an exact one-hour expiry.

==================================================
TC-CACHE-004 REQUIRED EXECUTION
==================================================

Use a controlled future-dated JWT and shortened token validity where supported.

Example controlled setup:

- Token validity: 20 seconds
- ttlBufferInSeconds: 5 seconds
- Effective cache TTL: approximately 15 seconds
- First token marker: TOKEN-A
- Refreshed token marker: TOKEN-B

Steps:

- T+0: request, token downstream count = 1.
- Before effective expiry, around T+10: count remains 1.
- After effective expiry, around T+16: count becomes 2.
- Immediate repeat around T+17: count remains 2.

Where the controlled setup is unavailable, include this exact precondition:

Execution requires approved WireMock setup or controlled Dev token configuration.

==================================================
TC-CACHE-005 REQUIRED EXECUTION
==================================================

Prove the five-second buffer:

- Original token remains valid until approximately T+20.
- Cache should expire around T+15.
- Request around T+14 reuses the token.
- Request around T+16 fetches a new token.
- Original token was still valid when refresh occurred.

Use a reasonable manual timing tolerance.

Do not require exact millisecond execution.

==================================================
TC-CACHE-006 REQUIRED EXECUTION
==================================================

Use controlled config data:

- Config TTL: 15 seconds
- Initial safe marker: VERSION-A
- Updated safe marker: VERSION-B

Steps:

1. Restart pod.
2. Request and cache VERSION-A.
3. Change controlled downstream response to VERSION-B.
4. Request before expiry.
5. Confirm VERSION-A remains in use.
6. Confirm downstream count remains 1.
7. Request after expiry.
8. Confirm VERSION-B is fetched.
9. Confirm downstream count becomes 2.
10. Immediately repeat.
11. Confirm VERSION-B is reused and count remains 2.

Do not require a screenshot of the complete config object.

==================================================
TC-CACHE-007 REQUIRED EXECUTION
==================================================

Use two confirmed test third-party IDs.

Steps:

1. Restart pod.
2. Request Third Party A.
3. Confirm A config marker.
4. Request Third Party B.
5. Confirm B config marker.
6. Repeat A.
7. Repeat B.
8. Confirm one initial config downstream call per unique third-party ID.
9. Confirm A never receives B’s data.
10. Confirm B never receives A’s data.

Do not replace this with a generic single-call count test.

==================================================
TC-CACHE-008 REQUIRED EXECUTION
==================================================

Use WireMock or a controlled downstream.

Steps:

1. Restart pod.
2. Configure token downstream to return HTTP 500.
3. Run business request.
4. Confirm documented error response.
5. Confirm token failure event.
6. Confirm no token-store event.
7. Configure a valid future-dated token response.
8. Repeat request.
9. Confirm token downstream is invoked again.
10. Confirm successful token is stored.
11. Repeat once more.
12. Confirm token downstream count does not increase.

==================================================
TC-CACHE-009 REQUIRED EXECUTION
==================================================

Use WireMock or a controlled downstream.

Steps:

1. Restart pod.
2. Configure config downstream to return HTTP 500.
3. Run business request.
4. Confirm documented failure response.
5. Confirm TP_CONFIG_CACHE_FETCH_FAILED.
6. Confirm no config-store event.
7. Configure successful VERSION-A response.
8. Repeat request.
9. Confirm downstream is called again.
10. Confirm VERSION-A is stored.
11. Repeat once more.
12. Confirm VERSION-A is served from cache.

==================================================
TC-CACHE-010 REQUIRED EXECUTION
==================================================

Inspect all existing Postman endpoints and include impacted business regression as separate numbered steps inside TC-CACHE-010.

At minimum inspect and include where present:

- Party
- Accounts
- Account by ID
- Balances
- Transactions
- Next transactions/pagination
- Accounts party
- Statements
- Statement by ID
- Statement file/download

For every endpoint provide:

Action:

- Exact Postman collection.
- Exact folder.
- Exact request.
- Exact execution steps.

Data:

- Method.
- URL/path.
- Existing environment variables.
- Required IDs.
- Required headers.
- Request-specific data.

Expected Result:

- Expected HTTP status.
- Expected response schema.
- Important stable fields.
- Whether token cache is used.
- Whether config cache is used.
- Expected relevant logs.
- No regression caused by the package upgrade.

Do not create separate Smoke test IDs.

==================================================
EVIDENCE REQUIREMENTS
==================================================

For every test, identify exactly what to attach.

Postman:

- Request name.
- URL and method.
- Safe visible headers.
- Request timestamp.
- HTTP status.
- Stable response fields.
- Correlation ID.

OpenShift:

- Pod name.
- Namespace.
- Test window.
- Filtered cache/token events.
- Cumulative event counts.

Splunk:

- Exact filtered search result.
- Pod and namespace.
- Test window.
- Expected event counts.

Do not request duplicate screenshots unless they prove different assertions.

==================================================
FINAL OUTPUT RESTRICTIONS
==================================================

Return only:

1. Common Preconditions
2. TC-CACHE-001 through TC-CACHE-010
3. Recommended execution order
4. Compact evidence checklist

Do not return:

- Endpoint-impact analysis narrative.
- Limitations section.
- Screenshot checklist as a separate document.
- Implementation suggestions.
- New logging design.
- Automated test code.
- Stand-up update.
- Additional test IDs.
- Generic test strategy.

Before returning, validate that:

- Every test uses Action, Data and Expected Result.
- Every step is manually executable.
- Exact existing Postman names are used.
- Exact existing variables are used.
- Exact confirmed log events are used.
- OpenShift and Splunk execution steps are readable.
- No full tokens or sensitive data are requested.
- Response time is not used as proof.
- DELETE /cache is not used as an affected-cache reset.
- TC-CACHE-002 uses an immediate repeat within two seconds.
- TC-CACHE-003 uses a separate ten-second regression wait.
- Stable response fields are compared instead of full body equality.
